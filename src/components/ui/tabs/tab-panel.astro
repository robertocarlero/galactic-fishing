---
type Props = {
  tabId: string;
  class?: string;
};

const { tabId, class: className = "" } = Astro.props;
---

<div
  role="tabpanel"
  data-tab-id={tabId}
  id={`panel-${tabId}`}
  class:list={["tab-panel w-full", className]}
>
  <slot />
</div>
